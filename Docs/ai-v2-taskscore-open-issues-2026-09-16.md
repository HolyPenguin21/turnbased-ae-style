# AI V2 TaskScore — consolidated outstanding audit issues (2026-09-16)

Status: FIXES IMPLEMENTED IN DRAFT PR #50, UNITY VALIDATION PENDING. This is the SINGLE TaskScore audit ledger; `ai-v2-taskscore-audit-2026-09-16.md` contains the original stage trace and synthetic examples. Baseline `master`: `cd773013a28869caa10fd9aa177df2aadf39471b` (2026-09-16). Production fixes for F1/F4/F5: commit `7e40cb74`; obsolete scoring cleanups C1–C6: `a1755487`; Base switch F2/F3: `09167bb`, plus follow-up safeguards in the succeeding hardening commit. Unity compilation/EditMode and game replay NOT RUN. No merge until Unity validation succeeds.

Rules: preserve one existing owner per semantic fact, game legality, actor identity, card and AP ownership, commitment hysteresis, WorthIt, and layer boundaries. No new vertical layer and no ad-hoc compensating score. Remove old formulas/fields/config only with a project-wide reference sweep, and change dependent tests in the same commit. All F1–F5 and C1–C6 changes are committed in the PR; runtime/Unity validation remains OPEN.

## Implemented versus pending

Implemented in PR: F1–F5 behavior and C1–C6 cleanup, with AP, Base income and Base switch regression sources. Pending: Unity compilation, EditMode execution and in-game turn-log replay; tests have NOT been executed. R1/R3/R4 remain evidence-gathering investigations, R2 intentionally skipped. Do not mistake GitHub Actions source-transform success or `git diff --check` for Unity test results.

## Confirmed behavioral defects — five (implemented, Unity validation pending)

**F1 [IMPLEMENTED / UNITY TEST PENDING] — AP overcommit during allocator repack.** `Assets/Scripts/Ai/V2/Allocation/ResourceAllocator.cs`, `AllocationSession.Pack`. Hard/Soft commitment ceiling checks `committedApSoFar + askAp > pool.Ap + eps` but fails to count `lockedTotal` provisioned AP. Pool3, provisioned Scout2, requested Raid3 can reach total5. Add `lockedTotal` to ceiling without changing pool/debit accounting. Regression added: `Assets/Editor/AiTaskScoreAllocatorRegressionTests.cs` (NOT RUN after fix). Prove no Raid funding and zero overdraft on lock→repack.

**F2 [IMPLEMENTED / UNITY TEST PENDING] — Base switch compares incompatible values.** `Assets/Scripts/Ai/V2/Strategy/StrategicPhaseA.cs`, `FulfillDemands/supersedingBaseSite`: new fully delivered `d.Value` compared with old `Economy.BuildValue` (site-only) plus hysteresis. Compare fresh full canonical incumbent `Value` if available, else valid saved `Economy.IntrinsicValue`, with challenger full `Value` and existing hysteresis. Do not repurpose `BuildValue`; preserve card, target, actor and reservation ownership. Test incumbent site40/net20 vs challenger site38/net34, hysteresis10; also challenger net29 and missing refreshed incumbent.

**F3 [IMPLEMENTED / UNITY TEST PENDING] — Base challenger is pruned before switch comparison.** `Assets/Scripts/Ai/V2/Strategy/Demand/DemandLayer.Economy.cs`, `EconomyDemands` sorts active Base first then `.Take(economyMaxExpansionBaseDemandsPerTurn)` (cap1); `StrategicPhaseA.supersedingBaseSite` searches emitted `demands` and never receives challenger. Repair candidate-selection and Phase-A/Continuity hand-off through EXISTING owners so a legitimate challenger can be evaluated, while emitting at most one executable Base demand and releasing old commitments transactionally. Do not simply raise cap or double-book hero/card. Integration test Demand→PhaseA→Continuity, positive/negative hysteresis and shared-resource contention. F2/F3 changed together in `09167bb`; source tests added, execution pending.

**F4 [IMPLEMENTED / UNITY TEST PENDING] — fictitious extraction-income loss on legitimate Base merge (user item 3).** `DemandLayer.Economy.AddBaseCandidates` computes `existingLoss = EconomicHexBenefit(site.LostExtractionIncome,0)` for `ConvertsOwnedExtractionSite` and adds it twice as identical site/full TaskScore slots (not double-folded, but both scores wrong). Actual `InfrastructureActions.CanMergeIntoResourceSite/TryFoundBase` admits only mergeable own sites and copies all existing `FacilitySlots` into the new Base; `WorldAnalysis.BaseUncollectedYield` already excludes existing building collection. REMOVE the fictitious loss from Base TaskScore/diagnostics, and remove associated dead legacy `ScoreBaseSite` extraction-loss calculation only after the reference sweep. Do not remove site-merge legality or actual building collection. Test a 1-income facility survives merge, net income before/after is unchanged absent new card Collect, `ExistingValueLoss=0`, and unchanged outputs are never charged as losses.

**F5 [IMPLEMENTED / UNITY TEST PENDING] — Base counts income already collected by own armies (user item 4).** `WorldAnalysis.BaseUncollectedYield` deducts building collection but not owned army collection. `StrategicCardEvaluator.BaseCardMarginalGain` then takes `min(1, remaining)` by card ability; `DemandLayer.AddBaseCandidates` uses it for total gain, per-type shortages, Payback and EconomyExpectedIncomeGain. Unlike Extraction, it ignores `IncomeProjection.MarginalOwnerCollectionAtHex`. Yield1 + uncontested own Collect army1 + new Collect Base1 means before owner income1 and after income1, NOT gain1. Use the existing canonical `IncomeProjection.MarginalOwnerCollectionAtHex` in the existing analysis/card-semantic owners for each card/type, respecting current own building capacity, own army collection and hostile contest. Derive total gain AND per-type TaskScore shortage, payback, expected income from the SAME net marginal values. Keep card semantics and avoid double-subtracting carried facilities. Test own army already fully harvesting, partially unharvested yield, existing facility carryover, multi-resource Base and contested hex. Align scalar/multi-resource `EconomicHexBenefit` through one folding algorithm after input semantics are made correct; formula dedup alone does not fix the false input.

## Approved structural cleanups — not proof of behavioral bugs

**C1 [CLEANED / UNITY TEST PENDING] — old extraction-deficit constant (user item 1).** Identify `economySiteDeficitValue` and related pre-TaskScore formula references across code, config, diagnostic and tests. Old weighting is NOT used to compute migrated Extraction `WorldTaskScore`. Remove genuinely unused legacy config/formula/test assertions; keep currently used `EconomyResourceStanding`, `ResourcePriority`, shortage calculations and starvation/reservations. Never delete gameplay demand data simply because the old scoring coefficient is unused.

**C2 [CLEANED / UNITY TEST PENDING] — resource-cluster legacy field (user item 2).** `WorldAnalysis.BuildEconomy` still calculates `EconomyResourceClusterValue` and populates `EconomyExtractionOpportunity.NearbyResourceClusterValue`, while `DemandLayer.EconomyDemands` does not include it in migrated TaskScore. Remove method, field, config knobs, production assignment and obsolete tests only after verifying no other consumer; preserve `BaseNetworkSynergy` if another feature consumes it. Do not silently put cluster value back into TaskScore or remove raw resource-hex knowledge.

**C3 [CLEANED / UNITY TEST PENDING] — old Base spacing SCORE only (user item 5).** `WorldAnalysis.BaseSpacingScore`, opportunity `SpacingScore`, legacy `ScoreBaseSite.reasonValue` weighting and TaskScore logs still exist; migrated Base TaskScore has no spacing slot. Remove unused numerical ramp, weighting/config, field and its diagnostic/test assumptions after reference sweep. KEEP `WorldAnalysis.MeetsBaseSpacing` and `economyBaseMinSpacing`, which enforce actual legal distance; KEEP the nearest-owned-home distance as independent strategic proximity.

**C4 [CLEANED / UNITY TEST PENDING] — eliminate obsolete second Base evaluator/legacy scoring (user item 9).** `StrategicCardEvaluator.BaseSiteValue` currently carries legacy `ReasonValue`, `IntrinsicBuildCost`, `ExtractionLossPenalty`, `StrategicValue`; `ScoreBaseSite` computes them via legacy `BaseHexYieldValue`, old weighted constants and spacing despite Demand only consuming raw `HexYield`, `GlobalEffect`, `Airfield`, `Exposure`. Replace with only live card-semantic raw facts or direct access to existing evaluator methods; remove legacy fields, formula, private `BaseHexYieldValue`, obsolete configuration and tests after checking all real references. Preserve `BaseGlobalEffectValue`, `BaseAirfieldValue`, `ThreatExposure`, card-specific Collect, other consumers and the one common TaskScore fold. Do not remove genuine `ResourceCostSum`, `IncomeProjection`, or live card-cost guards.

**C5 [CLEANED / UNITY TEST PENDING] — use the existing common proximity owner (user item 7).** All three Recon tasks and Economy already use `TaskScoreEvaluator.OwnTerritoryProximity`; `ReconObjectiveEvaluator.HomeDistance` is a compatibility/test-seam delegate to `TaskScoreEvaluator.NearestOwnedHomeDistance`. When tests are migrated, inline calls and remove redundant wrapper if there are no other consumers. Preserve notional home→target distance at objective enumeration and actual actor→destination distance after assignment: these are different phase inputs, NOT competing proximity scoring algorithms. Likewise do not delete route/path/ETA feasibility data while consolidating conversion.

**C6 [CLEANED / UNITY TEST PENDING] — one economic folding algorithm (user item 4).** `TaskScoreEvaluator.EconomicHexBenefit(float gain,float priority)` and `EconomicHexBenefit(IReadOnlyList<(Gain,Priority)>)` already share physical weighting; the multi-resource version correctly attributes shortage to each produced type, caps total deficit once. Optionally route a one-resource tuple through the collection implementation or retain a trivial adapter, removing only duplicated arithmetic. Require numeric equivalence for one type and independent shortage for multi-type. F5, not this overload, causes false Base income.

## Investigations only — no speculative score changes

**R1 — delivery/route proxy mismatch (prior audit and item 6).** Economy prices selected pinned hero's `SafeStepPathing` witness, Raid uses pinned mover activation plus axial hex distance, Recon enumerates with `ScoutCostModel.Estimate` nearest home before selecting actor/vantage, then actual `PairCost` and provisioning. Shared `TaskScoreEvaluator.Delivery` weights are already used: the distances are intentionally computed at different phases, but accurate route repricing requires planned-vs-actual telemetry. Do not replace legal actor-specific routes with straight-line home estimates. If logs prove biased acceptance, fix the appropriate existing estimator owner.

**R2 — mover opportunity cost Raid/Recon (user item 8: explicitly SKIP).** Economy prices borrowing an already committed mover; Raid sets zero, Recon omits the slot, while assignment/provisioning independently prevent actor conflicts. No proven incorrect choice without contested-mover log. No implementation planned now.

**R3 — durable Extraction lifetime under repeated contention.** Prior audit noted fallback to captured intrinsic score and no proven bounded termination for identical `MoverContended` repeats; record stable key, actor/route ownership, attempts, failure disposition and reservation ledger. Do not change commitment lifetime without a real trace.

**R4 — other existing calibration risks.** Raid reward/win weight, Base strategic geometry terms, Extraction payback horizon and Development/Production vs map-task stage differences remain unsupported tuning hypotheses; leave weights untouched without comparative real logs.

## Validation gate before marking resolved

For each changed file: identify exact owner and all references; compare fixed score against actual owner income and game transaction; preserve legal checks, AP/resource/card/actor reservation and continuity; add minimal Unity regression; verify source diff and static references; user runs Unity compilation/EditMode and game-turn logs. Update each issue's status with a real commit SHA and test result. All five fixes and six cleanups are implemented in draft PR #50; PR remains NOT READY until Unity compilation, EditMode tests and relevant game logs validate them.
