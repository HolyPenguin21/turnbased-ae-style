namespace Game.Core
{
    // Central place for scene name strings, so they're not duplicated/misspelled across
    // loader scripts. Must match the exact scene file names added to Build Settings.
    public static class SceneNames
    {
        public const string MainMenu = "MainMenu";
        public const string Game = "Game";
    }
}
